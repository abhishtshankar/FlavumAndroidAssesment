package com.example.flavumandroid.data

// An enum class defining keys for Flavum preferences
enum class FlavumPreferenceKeyEnum(val value: String) {

    // Key for a demo enum value
    DEMO_ENUM("demo_enum"),

    // Key for the user's type
    USER_TYPE("user_type"),

    // Key for the user's name
    USER_NAME("user_name"),

    // Key for the doctor's name
    DOCTOR_NAME("doctor_name"),

    // Key for the patient's name
    PATIENT_NAME("patient_name")
}
